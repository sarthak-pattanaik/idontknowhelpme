# Static Mode Environment Configuration

# Enable static mode (no backend)
VITE_STATIC_MODE=true

# Build Configuration
NODE_ENV=production

# Simulated API keys (not real keys)
VITE_SIMULATED_KEYS=true

# Base URL for static deployment
VITE_BASE_URL=/